<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accueil - Mon Site</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

    <% if flash[:success].present? %>
    <div class="alert alert-success" role="alert">
    <%= flash[:success] %>
    </div>
    <% end %>


    <div class="container mt-5">
    <div class="jumbotron text-center">
        <h1 class="display-4">Bienvenue sur EventBrite !</h1>
        <p class="lead">Trouve et partage des événements passionnants.</p>
        <a class="btn btn-primary btn-lg" href="/events/new" role="button">Créer un événement</a>
    </div>

    <div class="text-center">
        <img src="<%= asset_path('beyonce') %>" class="img-fluid rounded-start" alt="Beyoncé">
    </div>
    </div>

<h2 class="mb-4">Événements à venir :</h2>
<div class="row">
    <!-- Boucle pour afficher chaque événement -->
    <% @events.each do |event| %>
        <div class="col-md-4">
            <div class="card mb-4 shadow-sm">
                <img src="<%= asset_path('event_image') %>" class="card-img-top" alt="<%= event.title %>">
                <div class="card-body">
                    <h5 class="card-title"><%= event.title %></h5>
                    <p class="card-text"><%= event.description %></p>
                    <div class="d-flex justify-content-between align-items-center">
                        <div class="btn-group">
                            <%= link_to "Voir", event_path(event), class: "btn btn-sm btn-outline-secondary" %>
                        </div>
                        <small class="text-muted"><%= event.start_date.strftime("%d %B %Y") %></small>
                    </div>
                </div>
            </div>
        </div>
    <% end %>
    <!-- Fin de la boucle pour afficher chaque événement -->
</div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
